const isActive = true;
const subscription = true;
const isPrime = true;
if (isActive) {
  if (subscription) {
    if (isPrime) {
      console.log('look prime feature')
    } else {
      console.log('look free version')
    }
  }
}